{ ... }:
{
  # iTerm2 configuration
  # Note: iTerm2 is installed via Homebrew cask
  # Additional iTerm2-specific settings can be configured here
  # Most settings are managed through iTerm2's preferences UI

  home.file.".config/iterm2/.keep".text = ''
    # iTerm2 configuration placeholder
    # Custom color schemes and profiles can be placed here
  '';
}
